FROM python:3.11-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /app
RUN pip install --no-cache-dir fastapi==0.111.0 uvicorn==0.30.1
COPY morphcloud /app/morphcloud

EXPOSE 8080
CMD ["python", "-m", "morphcloud.serving.server", "--host", "0.0.0.0", "--port", "8080"]
