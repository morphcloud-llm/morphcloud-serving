# Kubernetes deployment
The chart in `../../helm/` deploys the control-plane scaffold only. See `../../helm/README.md`. GPU/node provisioning and the experiment vLLM data plane remain cloud-specific and are intentionally not fabricated in this repository.
